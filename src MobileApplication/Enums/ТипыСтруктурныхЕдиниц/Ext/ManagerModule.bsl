#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Функция возвращает представление типа структурных единиц.
// 
// Параметры:
// 	ТипСтруктурнойЕдиницы - ПеречислениеСсылка.ТипыСтруктурныхЕдиниц - тип структурной единицы.
// Возвращаемое значение:
// 	Строка - представление типа структурной единицы
Функция Представление(ТипСтруктурнойЕдиницы) Экспорт
	
	Если ТипСтруктурнойЕдиницы = Подразделение Тогда
		Возврат НСтр("ru = 'в подразделении'");
	КонецЕсли;
	
	Если ТипСтруктурнойЕдиницы = Розница Тогда
		Возврат НСтр("ru = 'на розничном складе'");
	КонецЕсли;
	
	Если ТипСтруктурнойЕдиницы = РозницаСуммовойУчет Тогда
		Возврат НСтр("ru = 'на розничном складе'");
	КонецЕсли;
	
	Возврат НСтр("ru = 'на складе'");
	
КонецФункции

#КонецОбласти

#КонецЕсли
